def ask_chatgpt(user_text: str) -> str:
    """
    Replace this function with your actual PC-side ChatGPT call.
    """
    if not user_text:
        return "I did not receive any text."

    return f"Stub response for: {user_text}"